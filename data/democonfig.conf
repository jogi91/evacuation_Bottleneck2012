% Input Parameters

% Spawning parameters
num_spawn_zones = 1

% Total number of agents for each Spawn zone
spawn_count_1 = 35

% Layout parameters:
num_exits = 3
exit_capacity_1 = 15
exit_capacity_2 = 10
exit_capacity_3 = 10

floor_build = singleSpawnMultiExit.png
meter_per_pixel = 0.08

% Video options
save_frames = 0
frame_basename = config1_frame

% Simulation parameters
dt = 0.05
duration = 120

% Force parameters:
tau = 0.2
A = 160
B = 1
k = 5e4
kappa = 100

% Agent paramters:
r_min = 0.25
r_max = 0.35
v0 = 0.8
v_max = 10
m = 80

% Parameters for desired field updates (after closing exits):
dfieldupdate_enable = 1  % 0 makes updates instantaneous,
                         % 1 makes them propagate the closing exit in a circle
dfieldupdate_speed = 2  % in m/s
